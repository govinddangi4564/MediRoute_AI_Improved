export const metadata = {
  title: "Symptoms | MediRoute AI",
  description: "Describe symptoms for AI-assisted emergency guidance.",
};

export default function SymptomsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

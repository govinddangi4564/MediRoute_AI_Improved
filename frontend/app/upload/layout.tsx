export const metadata = {
  title: "Reports | MediRoute AI",
  description: "Upload medical reports for simple AI-assisted explanation.",
};

export default function UploadLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
